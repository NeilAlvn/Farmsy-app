Quick update — account deletion is fully sorted.

Users can now delete their account and data right inside the app (Apple actually requires this for approval), and there's a public deletion page on the web for the store listing. Also set up an admin tool to manage deletion requests, so anything that comes in from signed-out users lands in a queue you can review.

Cutting on with the Play store paperwork now — data safety, the required declarations, all that. Getting through it. Will flag you when we're ready to actually submit.
