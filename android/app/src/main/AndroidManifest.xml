<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Google Play Billing. Play Console refuses to let you create in-app
         products until an uploaded build declares this, so it must ship. -->
    <uses-permission android:name="com.android.vending.BILLING" />

    <application
        android:name=".FarmsyApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Farmsy">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- Sentry's SDK auto-initializes from a ContentProvider before
             Application.onCreate, and hard-crashes the app if no DSN is set in
             the manifest. We init it ourselves in Observability (so a missing
             key disables it instead of killing the app), so turn the automatic
             one off. -->
        <meta-data android:name="io.sentry.auto-init" android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Referral links: farmsy.app/join?ref=CODE opens the app and the code
                 is held until signup. Without this the link only ever opened the
                 website, so an invited user who installed the app arrived with no
                 code at all and the referrer was never credited. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="farmsy.app" android:pathPrefix="/join" />
                <data android:scheme="https" android:host="www.farmsy.app" android:pathPrefix="/join" />
            </intent-filter>
        </activity>
    </application>
</manifest>
