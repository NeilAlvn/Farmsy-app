# Copy to android/secrets.properties (gitignored) and fill in.
# All PUBLIC client keys — safe to ship inside the APK, never committed.
MAPS_API_KEY=
REVENUECAT_KEY=
SENTRY_DSN=
POSTHOG_KEY=
POSTHOG_HOST=https://eu.i.posthog.com
