# Copy to android/secrets.properties (gitignored) and fill in.
# The Maps key is restricted to package app.farmsy.android + our signing SHA-1s,
# so it is safe to ship in the APK but never committed to git.
MAPS_API_KEY=
